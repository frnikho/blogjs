import React from "react";
import {Post} from "../types/Post";

interface PostProps {
    post: Post,
}

const Post: React.FC<PostProps> = ({post}) => {
    return (
        <div>
            
        </div>
    )
}

export default Post;
